{
  "plugins": [
    {
      "name": "td-mcp",
      "description": "Treasure Data API plugin for database management",
      "command": {
        "args": ["uv", "run", "mcp"],
        "env": {
          "TD_API_KEY": "${TD_API_KEY}"
        }
      }
    }
  ]
}
